[package]
name = "thiserror_"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = { version = "2.0.12", features = ["std"] }

[dev-dependencies]
common = { path = "../../../helpers/common" }
